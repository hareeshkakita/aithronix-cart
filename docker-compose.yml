version: "3.9"
services:
  cart-service:
    build: .
    container_name: aithronix-cart
    ports:
      - "8082:8082"
      - "5006:5005"
    environment:
      JAVA_TOOL_OPTIONS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005"
      SPRING_DATA_REDIS_HOST: redis-stack-server
      SPRING_DATA_REDIS_PORT: 6379
    networks:
      - app-network
    volumes:
      - ./src:/app/src

networks:
  app-network:
    external: true
